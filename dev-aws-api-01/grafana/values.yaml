grafana:
    ingress:
        enabled: "true"
        hosts: 
          - argocd-grafana.hazelcast.com
        annotations:
            kubernetes.io/ingress.class: "nginx"
    replicas: 1
    annotations:
        foo: bar